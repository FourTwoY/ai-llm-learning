# Day 15 - 第一次千问 API 调用

这个练习的目标是先跑通一次大模型 API 调用，不追求复杂功能。
起初用的是openai的，但是需要收费，有些麻烦，切换到了阿里云。切换到阿里云的时候
也遇到了问题，多模态大模型和纯文本大模型使用的response Generation不同
使得代码和大模型种类匹配后，代码得以正常运行

## 使用的接口

我使用的是阿里云百炼（DashScope）提供的通义千问 API。

## 使用的模型

- `qwen3-max-2026-01-23`

## 脚本文件

```text
day15_qwen_first_call.py

