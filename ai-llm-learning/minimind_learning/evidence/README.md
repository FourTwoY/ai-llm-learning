# Evidence 说明

这个目录只保存由实验记录整理出的摘要，不保存原始大文件。

原始大文件包括：

- `dataset/*.jsonl`
- `out/*.pth`
- `checkpoints/`
- `logs/`
- 训练产生的缓存目录

GitHub 不提交这些文件，是为了避免仓库过大、避免泄露训练产物，也让项目展示重点放在实验过程和结论上。

当前证据摘要来自 `tmp_minimind_records/`。该目录本身已加入 `.gitignore`，不应提交。
